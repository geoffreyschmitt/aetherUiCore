'use client';
import styled from 'styled-components';

export const RootElement = styled.span`
  font-family: var(--typography-font-family, Arial, Helvetica, sans-serif);
  font-size: var(--typography-font-size, 16px);
  line-height: var(--typography-line-height, 1.5);
  font-weight: var(--typography-font-weight, 400);
`;
