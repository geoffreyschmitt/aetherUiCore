'use client';
import styled from 'styled-components';

export const RootElement = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  .page-layout__footer {
    margin-top: auto;
  }
`;
