'use client';
import styled from 'styled-components';

export const RootElement = styled.div`
  width: 100%;
  max-width: var(--container-max-width, 100%);
  margin: 0 auto;
  padding: 0 var(--container-side-padding, 0);
`;
